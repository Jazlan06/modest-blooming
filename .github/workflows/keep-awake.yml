name: Keep Render Backend Awake

on:
  schedule:
    - cron: '*/7 * * * *'   # every 7 minutes
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          echo "💓 Pinging Render backend..."
          curl -fsS https://modest-blooming-bcknd.onrender.com/health || true
